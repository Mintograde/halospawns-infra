[package]
name = "replay-extractor"
version = "0.1.0"
edition = "2024"

[dependencies]
flate2 = "1.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
zstd = "0.13"

[profile.release]
codegen-units = 1
lto = "thin"
panic = "abort"
strip = true
