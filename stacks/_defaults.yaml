vars:
  project: halospawns
  region: us-east-1

terraform:
  backend_type: s3
  backend:
    s3:
      key: "{{ .vars.environment }}/{{ .component }}/terraform.tfstate"
      region: "{{ .vars.region }}"
      profile: "{{ .vars.profile }}"
      use_lockfile: true
